apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: main-namespace-production

resources:
  - ../../../base/observability

# patchesStrategicMerge:
#   - ../namespace.yaml  # se quiser forçar namespace em objetos existentes
  #- main-patch.yaml    # patches específicos do overlay

commonLabels:
  env: production
