global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  - /etc/prometheus/rules/*.yaml

scrape_configs: []
# Serão mesclados dos arquivos em targets/ e exporters/