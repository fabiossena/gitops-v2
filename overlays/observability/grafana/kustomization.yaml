apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: observability

commonLabels:
  app.kubernetes.io/name: grafana

resources:
  - configmap-dashboard-provider.yaml
  - configmap-dashboard-ids.yaml
  - configmap-datasources.yaml
  - secret-admin.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml