apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-app
spec:
  rules:
    - host: teste.com.br
      http:
        paths:
          - path: /main
            pathType: Prefix
            backend:
              service:
                name: main-app
                port:
                  number: 80
