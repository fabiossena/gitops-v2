apiVersion: apps/v1
kind: Deployment
metadata:
  name: main-app-production
spec:
  template:
    spec:
      containers:
        - name: php
          env:
            - name: APP_ENV
              value: production
