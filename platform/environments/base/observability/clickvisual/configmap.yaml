apiVersion: v1
kind: ConfigMap
metadata:
  name: clickvisual-config
data:
  default.toml: |
    [mysql]
    dsn = "${MYSQL_USER}:${MYSQL_PASSWORD}@tcp(mysql.production-database.svc.cluster.local:33060)/clickvisual?charset=utf8mb4&parseTime=True&loc=Local"

    [clickhouse]
    host = "clickhouse"
    port = 8123
    database = "default"
