apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml

# namespace: telescope

configMapGenerator:
- name: telescope-config
  files:
    - configmap.yaml

secretGenerator:
- name: telescope-secret
  files:
    - secret.yaml
