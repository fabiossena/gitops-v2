apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-observability-{{ overlay }} 
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ gitops_repo_url }}
    targetRevision: main
    path: overlays/{{ overlay }}/observability
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: {{ overlay }}-namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true      